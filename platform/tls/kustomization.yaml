resources:
  # Existing CA (current)
  - ca-selfsigned-issuer.yaml
  - ca-certificate.yaml
  - clusterissuer-lan-ca.yaml

  # Enterprise PKI chain
  - root-issuer.yaml
  - root-ca-cert.yaml
  - root-ca-issuer.yaml
  - intermediate-ca-cert.yaml
  - clusterissuer-intermediate.yaml

  # Leaf certs
  - wildcard-lan.yaml
