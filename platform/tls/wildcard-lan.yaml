apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-lan
  namespace: hello
spec:
  secretName: wildcard-lan-tls
  dnsNames:
    - "*.lan"
    - "lan"
  issuerRef:
    name: lan-ca
    kind: ClusterIssuer
